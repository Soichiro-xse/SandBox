services:
  mirofish:
    build: .
    container_name: mirofish
    env_file:
      - .env
    ports:
      - "${PORT:-5001}:${PORT:-5001}"
    restart: unless-stopped
    volumes:
      - ./backend/uploads:/app/backend/uploads
